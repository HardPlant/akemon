function DollBattle(battleType, playingIdx, trainers, phases, metadata) {
    this.battleType = battleType;
    this.playingIdx = playingIdx;
    this.trainers = trainers;
    this.phases = phases;
    this.metadata = metadata;
}